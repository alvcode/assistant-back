meta {
  name: note-list
  type: http
  seq: 5
}

get {
  url: {{domain}}/api/notes?sort_order=desc
  body: none
  auth: inherit
}

params:query {
  sort_order: desc
  ~category_id: 6
  ~sort_by: updated_at
}
